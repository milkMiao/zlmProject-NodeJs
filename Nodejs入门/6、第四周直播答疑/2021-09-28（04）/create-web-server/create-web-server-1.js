#!/usr/bin/env node
// 注：#! 行 必须写在第一行，指定该脚本解析器路径
// /usr/bin/env => env: 获取环境变量信息
// /usr/bin/env node => env | grep PATH => 从环境变量 PATH 中查找 node 并执行
// 使用 env 查找 node，比直接指定 node 路径要好，因为不是所有人的 node 安装路径都是同一个


console.log(`hello`)

